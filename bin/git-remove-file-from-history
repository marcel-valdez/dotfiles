#!/usr/bin/env bash

if [ $# -lt 1 ]; then
  echo "Script requires the name of the file to remove, but no parameters were given"
  exit 1
fi

git filter-branch --force --index-filter \
"git rm --cached --ignore-unmatch $*" \
--prune-empty --tag-name-filter cat -- --all
