#!/bin/bash

function pandoc() {
  /cygdrive/c/ProgramData/chocolatey/bin/pandoc.bat $1 $2 $3 $4 $5 $6 $7 $8 $9
}

function notify-tmux() {
  for i in $(seq 1 5)
  do
    tmux display-message "$1"
    sleep 2
  done

  tmux display-message -p "$1"
}
