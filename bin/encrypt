#!/usr/bin/env bash
file=$1

if [ "$2" == "" ]; then
  openssl enc -aes-256-cbc -salt -in "$file" -out "$file.enc" #-pass file:$HOME/.ssh/key.bin
else
  pass_phrase="$2"
  openssl enc -aes-256-cbc -salt -in "$file" -out "$file.enc" -k $pass_phrase
fi
